# v326 handoff - Debug Mode, Inn opening, home follow-up

Workspace: C:/Users/user/projects/IJLG
Branch: codex/inn-learning-redesign. Do not merge into master.
Status: local, uncommitted, not pushed. Preserve earlier v324/v325 edits and
the user-owned .codex-remote-attachments directory. Ask before committing/pushing.

## Implemented

- Opening Menu has Debug Mode On/Off. Its URL is ?debug=1.
- Debug starts at character selection and Entrance, not after them.
- Normal keys lanternAlley.v2/v3 and debug keys lanternAlley.debug.v2/v3 are
  separate. Exit debug returns to normal progress. Debug exports are rejected
  by normal-mode import.
- All catalog furniture and wallpapers, the cat, and planted/mature garden
  species are available. Placeholder art is deliberately included for testing.
- Debug reload/import adds missing stock while preserving placed items, plant
  IDs and active wallpaper. Reset restocks only the debug save.
- Skip question/stage reuse the existing training controls; episode questions
  retain Skip question. Debug events do not initialize/send analytics or alter
  normal consent preferences.
- Set available=false in debug-mode.js to hide the new mode and ignore debug=1.
  Legacy skip/unlockall URL aids still exist; they are not isolated test saves.
- Inn acceptance now leads to one Day 1 board and the guided first task.
  No unsupported cold attempt or immediate duplicated first task. Old cold-start
  saves resume into Learn. Existing authored audio and dormant cold-open code
  remain; normal navigation no longer enters that phase.
- v325 paper-panel wallpaper masks are now embedded in CSS, removing external
  SVG-request dependency. Original mask SVGs remain as editable source assets.
  Keep source masks and embedded CSS copies in sync if changing their geometry.
- Cache, page URLs and manifest icon URLs are v326.

## Verification

- 11 focused debug/guided-opening/wallpaper/telemetry tests pass.
- Final complete suite including the import regression passed 525/525; log:
  C:/Users/user/AppData/Local/Temp/lantern-v326-verified-tests.txt
- Final syntax checks and git diff --check pass.
- Focused code review found the import/restock bug; fixed with a regression.
- No live browser/mobile verification of v326. Local server was unavailable.
  Do not describe fake-DOM tests as browser or visual proof.

## Still open - user specifically requested

1. Wallpaper: user says ALL designs fail to show. Local interaction tests show
   design selection/purchase/return-to-plain updating the rendered markup and save.
   Embedded masks address one possible loading failure, not a confirmed live
   diagnosis. Need the user's exact URL/build and a live reproduction. Asked
   which URL/designs; reply specified all designs but did not give the URL.
2. Interior looks small / objects too big. Do not arbitrarily shrink everything.
   Inspect actual object sizes against tatami and check desktop/mobile camera.
   Some vector placeholder objects use inflated layout widths on purpose.
3. Shelf organization: home-room.js defines shelf at (50,72) and tokonoma at
   (70,74), both kind shelf, but the background has neither physical support.
   Small objects currently rest at the back floor. These repo assets were viewed:
   - assets/home/decor/display-shelf-staggered-v1.webp
   - assets/home/decor/display-cabinet-kiri-v1.webp
   Both are usable transparent wooden furniture; neither is wired into the
   decor catalog. No new images needed for this approach.
   Proposed next design: place a shelf/cabinet against a side/back wall, give
   small objects real shelf-top slots, keep the doorway clear, and calibrate
   large furniture separately. This layout is not implemented or approved yet.
4. Live check opening Menu, Debug banner/exit, both save modes, Inn right/wrong
   paths, wallpaper selection and mobile home layout. Then ask before pushing.

## Useful checks

- node --test
- node --test --test-name-pattern="debug |guided |wallpaper selections|legacy cold|imported normal" walkthrough.test.mjs telemetry.test.mjs
- node --check app.js
- git -c core.safecrlf=false diff --check

Current change files: debug-mode.js, app.js, index.html, styles.css, telemetry.js,
sw.js, manifest.webmanifest, walkthrough.test.mjs, telemetry.test.mjs,
learning-progress.test.mjs, home-wallpaper.test.mjs, TESTING.md, CHANGELOG.md,
PROJECT-HANDOFF.md. Many other dirty files predate this task; preserve them.
